# Production environment configuration
# This file is read automatically by Create React App during `npm run build`
# NOTE: This is only used as a fallback. The frontend auto-detects the API URL
# from window.location.hostname at runtime.

REACT_APP_API_URL=http://localhost:8000
